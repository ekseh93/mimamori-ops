# Pipeline handoffs

운영 기준은 `../07-model-pipeline.ko.md`를 따른다. 실제 작업 위치와 ID는 원본 프로젝트의 `registry.json`이 기준이다.

각 워크트리의 파일이 실시간으로 공유되는 것은 아니다. 설계 패킷, 구현 보고, 검증 보고를 커밋하고 정확한 SHA와 함께 다음 작업에 메시지로 전달한다.

현재 단계: 역할 작업 생성·연결. 새 기능 개발을 실행한 상태는 아니다.
